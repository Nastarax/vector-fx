# DISABLED: GitHub Actions runner IPs are universally Cloudflare-blocked by
# Investing.com (every request returns 403 from hosted runners). Same problem
# we already accepted for ForexFactory. The Investing mPMI cache is refreshed
# locally via Windows Task Scheduler (or by manual `python scripts/refresh_investing.py`
# run on Yanaël's machine), then committed and pushed so the hourly heatmap
# workflow can read it.
#
# Kept as a file (instead of deleted) for documentation. To re-enable, add the
# `on:` and `jobs:` sections back. But don't bother unless GitHub runners stop
# being Cloudflare-flagged, which is unlikely.

name: Daily Investing mPMI Refresh (disabled)
